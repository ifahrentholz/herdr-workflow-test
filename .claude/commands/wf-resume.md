---
description: Show where the workflow left off; suggest the next command
---

Run `bash scripts/wf.sh resume` and pass the output to the user. After showing it, ask whether to continue with `/wf-next` or pause.

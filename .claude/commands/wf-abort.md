---
description: Reset the workflow state (issues / local task files are preserved)
---

Run `bash scripts/wf.sh abort` and pass through the interactive confirmation. If the user clearly wants no further prompt, pass `--yes`.

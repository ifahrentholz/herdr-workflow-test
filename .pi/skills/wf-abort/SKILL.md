---
name: wf-abort
description: Reset the workflow state (issues / local task files are preserved)
---

```bash
bash scripts/wf.sh abort
```

Confirm with the user before passing `--yes`. The script also prompts interactively.
